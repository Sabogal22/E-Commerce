---
// Importar iconos de Lucide (si los tienes instalados)
// Si no, puedes usar emojis o instalar: npm install lucide-react
---

<div class="relative overflow-hidden">
  <!-- Fondo con gradiente -->
  <div class="absolute inset-0 bg-gradient-to-br from-red-50 via-white to-blue-50"></div>
  
  <!-- Elementos decorativos -->
  <div class="absolute top-10 left-10 w-72 h-72 bg-red-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
  <div class="absolute top-40 right-10 w-72 h-72 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
  <div class="absolute -bottom-8 left-1/2 w-72 h-72 bg-yellow-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-4000"></div>

  <div class="relative z-10 container mx-auto px-4 py-16 md:py-24">
    <!-- Header -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center justify-center p-3 bg-red-100 rounded-full mb-6">
        <div class="w-16 h-16 bg-red-600 rounded-full flex items-center justify-center">
          <span class="text-white text-2xl font-bold">🏍️</span>
        </div>
      </div>
      
      <h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-6">
        Bienvenido a 
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-600 to-orange-500">
          MotoRepuestos
        </span>
      </h1>
      
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Tu tienda confiable de repuestos originales para motos. 
        <span class="font-semibold text-red-600">Calidad garantizada</span> y 
        <span class="font-semibold text-blue-600">envíos a todo el Perú</span>.
      </p>
    </div>

    <!-- Estadísticas destacadas -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
      <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-2xl transition-shadow">
        <div class="text-4xl text-red-600 mb-4">🔧</div>
        <h3 class="text-2xl font-bold text-gray-800 mb-2">500+</h3>
        <p class="text-gray-600">Repuestos diferentes</p>
        <p class="text-sm text-gray-500 mt-2">Para todas las marcas y modelos</p>
      </div>
      
      <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-2xl transition-shadow">
        <div class="text-4xl text-blue-600 mb-4">🚚</div>
        <h3 class="text-2xl font-bold text-gray-800 mb-2">24h</h3>
        <p class="text-gray-600">Envío express</p>
        <p class="text-sm text-gray-500 mt-2">Envío rápido a todo el país</p>
      </div>
      
      <div class="bg-white rounded-2xl p-6 shadow-lg hover:shadow-2xl transition-shadow">
        <div class="text-4xl text-green-600 mb-4">⭐</div>
        <h3 class="text-2xl font-bold text-gray-800 mb-2">4.9/5</h3>
        <p class="text-gray-600">Calificación</p>
        <p class="text-sm text-gray-500 mt-2">Clientes satisfechos</p>
      </div>
    </div>

    <!-- CTA principal -->
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold text-gray-800 mb-6">
        ¿Listo para encontrar lo que necesitas?
      </h2>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href="/productos" 
          class="inline-flex items-center justify-center bg-red-600 hover:bg-red-700 text-white font-bold py-4 px-8 rounded-xl text-lg transition-all transform hover:-translate-y-1 hover:shadow-2xl"
        >
          <span>Ver Catálogo Completo</span>
          <span class="ml-2">→</span>
        </a>
        <a 
          href="/ofertas" 
          class="inline-flex items-center justify-center bg-white text-red-600 border-2 border-red-600 hover:bg-red-50 font-bold py-4 px-8 rounded-xl text-lg transition-all transform hover:-translate-y-1"
        >
          <span>🔥 Ver Ofertas Especiales</span>
        </a>
      </div>
    </div>

    <!-- Pasos para comprar -->
    <div class="bg-white rounded-3xl shadow-xl p-8 md:p-12 mb-16">
      <h2 class="text-3xl font-bold text-center text-gray-800 mb-12">
        Compra en 3 pasos fáciles
      </h2>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-3xl">1️⃣</span>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Busca tu repuesto</h3>
          <p class="text-gray-600">
            Encuentra por marca, modelo o categoría
          </p>
        </div>
        
        <div class="text-center">
          <div class="w-20 h-20 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-3xl">2️⃣</span>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Agrega al carrito</h3>
          <p class="text-gray-600">
            Revisa stock y precios con promociones
          </p>
        </div>
        
        <div class="text-center">
          <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-3xl">3️⃣</span>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Recibe en casa</h3>
          <p class="text-gray-600">
            Envío rápido y seguro a tu domicilio
          </p>
        </div>
      </div>
    </div>

    <!-- Marcas que trabajamos -->
    <div class="text-center">
      <h3 class="text-2xl font-bold text-gray-800 mb-8">
        Marcas que manejamos
      </h3>
      <div class="flex flex-wrap justify-center items-center gap-8 md:gap-12">
        {['YAMAHA', 'HONDA', 'SUZUKI', 'KAWASAKI', 'BAJAJ', 'DUCATI'].map(marca => (
          <div class="bg-gray-50 px-6 py-3 rounded-lg hover:bg-white hover:shadow-lg transition">
            <span class="text-gray-700 font-semibold">{marca}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- Mensaje final -->
    <div class="mt-16 text-center">
      <div class="inline-block bg-gradient-to-r from-red-600 to-orange-500 text-white px-6 py-3 rounded-full mb-6">
        <span class="font-bold">✨ ¡NUEVO! ✨</span>
      </div>
      <p class="text-gray-700 text-lg">
        ¿Necesitas ayuda para encontrar el repuesto correcto?
        <a href="/contacto" class="text-red-600 font-semibold hover:underline ml-2">
          Contáctanos por WhatsApp
        </a>
      </p>
    </div>
  </div>

  <!-- Animación CSS para los blobs -->
  <style>
    @keyframes blob {
      0% {
        transform: translate(0px, 0px) scale(1);
      }
      33% {
        transform: translate(30px, -50px) scale(1.1);
      }
      66% {
        transform: translate(-20px, 20px) scale(0.9);
      }
      100% {
        transform: translate(0px, 0px) scale(1);
      }
    }
    
    .animate-blob {
      animation: blob 7s infinite;
    }
    
    .animation-delay-2000 {
      animation-delay: 2s;
    }
    
    .animation-delay-4000 {
      animation-delay: 4s;
    }
    
    .line-clamp-2 {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  </style>
</div>